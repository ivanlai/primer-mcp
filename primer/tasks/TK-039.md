---
id: TK-039
title: Reconcile CLAUDE.md gate claims with guidance-based behaviour
created: 2026-08-12
updated: 2026-08-12
blocked_by: []
external_ref: {}
type: task
status: verified
story_id: ST-022
testable_outcome: CLAUDE.md (and any README/docs text derived from it) no longer
  claims verify_task-before-complete_task is server-enforced; the wording matches
  the shipped nudge behaviour.
completed_notes: Removed verify-before-complete from enforced gates in CLAUDE.md and architecture.md;
  added it to nudges list; fixed "required" to "recommended" in dev workflow
verified_evidence: Docs updated across CLAUDE.md, architecture.md, epic-001.md; README already correct — e69daf9
---

## Parent Story
[[ST-022]]

## What to do
**What's gone wrong:** CLAUDE.md still claims gate 3 is enforced — it lists "Cannot call `verify_task` before `complete_task` has been called" under "Workflow gates (enforced by the server — never bypass)". But `verify_task` (tickets.py:441) deliberately proceeds from todo/in-progress with only a nudge — ST-021 shifted the workflow from enforcement to guidance and this gate was softened as part of that. The checked-in doc now misdocuments shipped behaviour, pre-release.

Fix the doc, not the code: update the CLAUDE.md gate list (and check docs/architecture.md and the README for the same claim) so only the gates the server actually enforces are listed as enforced. The two hard gates that remain are the parent-Epic requirement for ADRs and the parent-Story requirement for Tasks.

## Testable Outcome
CLAUDE.md (and any README/docs text derived from it) no longer claims verify_task-before-complete_task is server-enforced; the wording matches the shipped nudge behaviour.

## Dependencies
- (none)

## Completion Notes
Audited CLAUDE.md, docs/architecture.md, and README.md for claims that verify_task-before-complete_task is server-enforced. Found the claim in three places across two files (README was already correct):

1. **CLAUDE.md "Workflow gates" section** — removed gate 3 from the numbered list, added a prose note that two-phase completion is recommended/nudged but not enforced.
2. **CLAUDE.md "Development workflow" section** — changed "Both are required" to "Both are recommended" for the two-phase completion bullet.
3. **docs/architecture.md "Workflow Gates" section** — removed gate 3 from hard gates, added a verify-before-complete nudge bullet to the existing nudges list.
4. **docs/architecture.md "Ticket Lifecycle" section** — changed "requires status `completed`" to "recommends status `completed` (nudging if skipped)".
5. **docs/epic-001.md ST-006 description** — changed "requires" to "recommends (nudges if skipped)" and "fails with an actionable error" to "nudges".

## Verification Evidence
