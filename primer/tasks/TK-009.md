---
id: TK-009
title: get_ticket, list_tickets and update_ticket
created: 2026-08-09
updated: 2026-08-09
blocked_by: []
external_ref: {}
type: task
status: todo
story_id: ST-007
testable_outcome: 'test_query.py passes: rejects status changes on terminal tickets
  while allowing completed -> in-progress, rejects cycle-creating edges leaving the
  file byte-identical, rejects unknown IDs, sets external_ref, and get_ticket reports
  outgoing edges it does not store.'
completed_notes: ''
verified_evidence: ''
---

## Parent Story
[[ST-007]]

## What to do
Add query.py with the three read/update tools. update_ticket handles status, blocked_by, body_sections and external_ref, validating referenced IDs and rejecting cycles before writing anything. Also wire the blocked_by edges between this story's own tasks, which could not be set at creation time.

## Testable Outcome
test_query.py passes: rejects status changes on terminal tickets while allowing completed -> in-progress, rejects cycle-creating edges leaving the file byte-identical, rejects unknown IDs, sets external_ref, and get_ticket reports outgoing edges it does not store.

## Dependencies
- (none)

## Completion Notes


## Verification Evidence
