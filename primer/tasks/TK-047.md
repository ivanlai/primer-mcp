---
id: TK-047
title: Auto-init store when plan_epic is called on uninitialized project
created: 2026-08-13
updated: 2026-08-13
blocked_by: []
external_ref: {}
type: task
status: verified
story_id: ST-020
testable_outcome: Calling plan_epic on a directory with no primer/ creates the store
  and the epic in one call, returning both init and epic creation output
completed_notes: plan_epic auto-inits primer/ via guard clause; README rewritten for
  user-first UX; snippet condensed and synced
verified_evidence: 255 passed, mypy clean, ruff clean (commit 022f7c4)
---

## Parent Story
[[ST-020]]

## What to do
In plan_epic (tickets.py), detect when primer/ doesn't exist and auto-initialize using project_dir.name as the project name, instead of raising a GateError. Prepend the init output lines to the epic creation response so the agent sees what happened. Update the existing test that expects GateError to verify auto-init instead. Update the README quickstart to show plan_epic as the first step (init_project becomes optional/advanced).

## Testable Outcome
Calling plan_epic on a directory with no primer/ creates the store and the epic in one call, returning both init and epic creation output

## Dependencies
- (none)

## Completion Notes
Guard clause in `plan_epic` checks `(project_dir / PRIMER_DIR).is_dir()` — if missing, calls `init_project(project_dir, project_dir.name)` and prepends the output. No try/catch, no new abstractions. Test updated to verify auto-init creates both store and epic. README rewritten: quickstart leads with user experience instead of tool sequences, stale `get_next_action` renamed to `list_actionable`, "CLAUDE.md snippet" section renamed to "Agent instructions" covering both CLAUDE.md and AGENTS.md, dogfood section updated for auto-init. WORKFLOW_SNIPPET in project.py synced with README copy.

## Verification Evidence
255 passed, mypy clean, ruff clean (commit 022f7c4)
