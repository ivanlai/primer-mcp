---
id: TK-007
title: Schema and graph layer
created: 2026-08-09
updated: 2026-08-09
blocked_by: []
external_ref: {}
type: task
status: verified
story_id: ST-007
testable_outcome: 'test_graph.py and test_models.py pass: edges built from blocked_by,
  cycles detected on dependency edges and self-edges but not on a child story blocking
  its parent epic, derived done guarded against vacuous done, sort_key orders past
  ID 999. The migrated store still loads.'
completed_notes: 'Dropped `blocks` from TicketBase and narrowed the edge-ID validator
  to blocked_by, then stripped the now-invalid key from all 32 migrated tickets. Added
  graph.py (load_tickets, find_path, dependency_graph, find_cycle, children_of, derive_status,
  recompute_parents, sort_key) — dependency edges only, no hierarchy in the DiGraph.
  Two symbols moved to break the import cycle graph.py would otherwise have with tickets.py:
  GateError to a new errors.py, and SUBDIR_FOR_TYPE to project.py beside SUBDIRS,
  which now derives from it.'
verified_evidence: 'pytest: 124 passed (was 91; 33 new tests in test_graph.py plus
  the blocks-is-gone case in test_models.py). mypy strict: clean across 9 source files.
  ruff check and ruff format: clean on all files this task touches. Dogfooded against
  the real 32-ticket store: it loads through the new layer, the graph reports 32 nodes
  and no cycle, and derive_status independently computes ST-006 -> done, ST-007 ->
  not done, EP-001 -> not done, matching the store on disk.'
---

## Parent Story
[[ST-007]]

## What to do
Drop `blocks` from TicketBase and narrow the edge-ID validator to blocked_by, then strip the now-invalid `blocks:` key from every migrated ticket. Add graph.py: load_tickets, find_path, dependency_graph, find_cycle, derive_status, recompute_parents, sort_key. Dependency edges only — no hierarchy edges until the export_graph story has a renderer to feed.

## Testable Outcome
test_graph.py and test_models.py pass: edges built from blocked_by, cycles detected on dependency edges and self-edges but not on a child story blocking its parent epic, derived done guarded against vacuous done, sort_key orders past ID 999. The migrated store still loads.

## Dependencies
- (none)

## Completion Notes
Dropped `blocks` from TicketBase and narrowed the edge-ID validator to blocked_by, then stripped the now-invalid key from all 32 migrated tickets. Added graph.py (load_tickets, find_path, dependency_graph, find_cycle, children_of, derive_status, recompute_parents, sort_key) — dependency edges only, no hierarchy in the DiGraph. Two symbols moved to break the import cycle graph.py would otherwise have with tickets.py: GateError to a new errors.py, and SUBDIR_FOR_TYPE to project.py beside SUBDIRS, which now derives from it.
## Verification Evidence
pytest: 124 passed (was 91; 33 new tests in test_graph.py plus the blocks-is-gone case in test_models.py). mypy strict: clean across 9 source files. ruff check and ruff format: clean on all files this task touches. Dogfooded against the real 32-ticket store: it loads through the new layer, the graph reports 32 nodes and no cycle, and derive_status independently computes ST-006 -> done, ST-007 -> not done, EP-001 -> not done, matching the store on disk.
